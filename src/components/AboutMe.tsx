import AboutMeHeader from "@/utils/AboutMeHeader";
import React from "react";

const AboutMe: React.FC = () => {
	return (
		<section className="mt-10 min-h-screen bg-black px-10">
			<AboutMeHeader />
		</section>
	);
};

export default AboutMe;
